#ifndef DEF_DEFINE_HPP
#define DEF_DEFINE_HPP

class Define {
  public:
    static const int WIN_H;
    static const int WIN_W;

    static const float PI;

    enum eLevel {
      Easy,
      Normal,
      Hard,
    };
};

#endif